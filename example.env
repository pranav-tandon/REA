#USE THIS AS YOUR .ENV FILE WITH YOUR API KEYS

# MongoDB Configuration
# Define the MongoDB URI for connecting to your real estate database.
MONGO_URI=mongodb://localhost:27017/real_estate_db

# Ollama Configuration (used in local mode)
# When running locally with DeepSeek via Ollama, these variables specify
# the API base URL and the model name to use.
OLLAMA_API_BASE=http://localhost:11434
OLLAMA_MODEL_NAME=deepseek-r1

# DeepSeek API / GPT Configuration
# Set your DeepSeek API key for API mode. To use GPT mode instead of DeepSeek,
# change the key to "gpt" (and ensure that OPENAI_API_KEY is provided below).
DEEPSEEK_API_KEY=your_key_here 

# Optional: Temperature setting for model responses.
# Adjust this value (typically between 0 and 1) to control response creativity.
TEMPERATURE=0.7

# DeepSeek API Base URL
# Used only in API mode when DEEPSEEK_API_KEY is not "gpt".
DEEPSEEK_API_BASE=https://api.deepseek.com/v1

# OpenAI API Key (only used when DEEPSEEK_API_KEY is set to "gpt")
# Provide your OpenAI API key here if you wish to switch to GPT mode.
OPENAI_API_KEY=your_openai_api_key_here 